//! Local credentials for simulator testing.
//! Copy this file to LocalCredentials.mc and fill in your PocketCasts credentials.
//! LocalCredentials.mc is gitignored — your credentials will never be committed.
module LocalCredentials {
    const EMAIL = "your-email@example.com";
    const PASSWORD = "your-password-here";
}

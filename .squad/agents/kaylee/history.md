# Project Context

- **Owner:** Jarod Aerts
- **Project:** YoCasts — a Garmin watch client for the PocketCasts podcast app
- **Stack:** Garmin Connect IQ (Monkey C), with existing C#/.NET API reverse-engineering code as reference
- **Created:** 2026-04-11

## Core Context

**Current Status (2026-04-14):** Garmin app scaffolding complete. Phase 1 offline caching live. Home menu redesigned with split-dock layout — build passes strict linting. Simulator and physical hardware testing pending for Phase 0.

**UI Architecture (finalized):**
1. **App type:** AudioContentProviderApp — enables Music widget entry point + Garmin native playback controls.
2. **Home screen:** Custom HomeMenuView with split-dock design (Y=0–260: scrollable Queue/Podcasts/Settings pills; Y=260–390: fixed Now Playing dock).
3. **List screens:** Menu2 for Queue, Podcasts, Episodes (auto-scaling layout for round/rectangular displays).
4. **Now Playing:** Full-screen episode info view with artwork, progress bar, timestamps, play/pause button.
5. **Settings:** Placeholder screen for future playback speed, auto-download toggles.

**Device Target:**
- Venu 4 41mm only (390×390 AMOLED, capacitive touch + 2 buttons, 768 KB memory)
- Modern SDK (4.2+) enables rich caching and custom drawing

**Offline & Sync (Phase 1 complete):**
- CacheManager module wraps Application.Storage with typed save/load, 'yc_' key prefixes, cachedAt timestamps
- CachedPodcastService decorator wraps IPodcastService, loads cached data on init, delegates fetchAll() only on phone connection
- TTLs: queue 5min, podcasts 30min, episodes 1hr (revalidation hints, stale data always served)
- Phase 2 adds position tracking (15s intervals, adaptive battery scaling)
- Phase 3 adds audio download (via SyncDelegate, sequential, no resume in v1)

**Known Constraints & Workarounds:**
- Menu2 cannot dynamically add items post-construction (episode lists show "Loading..." on first visit, require back-and-re-enter)
- No podcast artwork in v1 (memory budget)
- maxSdkVersion: 4.2.99 to allow future SDK versions without manifest edits

**Completed Implementations:**
- `HomeMenuView.mc` (split-dock with Y-based touch zones, scrolling viewport, 80px pills, 280px uniform width)
- `CacheManager.mc` (module with typed getters/setters, clearValues() → selective delete in Phase 2)
- `CachedPodcastService.mc` (decorator pattern, read-through TTL cache, offline instant UI)
- `MockPodcastService.mc` (pre-loaded data for testing, IPodcastService interface contract)

## Learnings

<!-- Append new learnings below. Each entry is something lasting about the project. -->

**[2026-04-11 to 2026-04-16 Core Context Summarization]** Historical development phases consolidated: Phase 0 (AudioContentProviderApp migration, build fixes, strict linting) completed. Phase 1 (offline caching with CacheManager + CachedPodcastService decorator pattern) complete. Auth uses `/user/login_pocket_casts` + proactive token refresh. Device targeting: Venu 4 41mm only (390×390, 768KB memory). API research: `/up_next/list` returns dictionary structure (order array + episodes map), `/user/podcast/episodes` returns status only (no titles), `/sync/update_episode` for position sync, `/user/in_progress` for bulk reconciliation. Offline sync: 3-state connectivity (Wi-Fi/Phone BT/Fully Offline), `connectionAvailable` primary check, Wi-Fi direct works natively. Implementation phases A-E: A (changelog + position tracking, 2-3 days), B (sync engine, 4-5 days), C (audio downloads via SyncDelegate), D (media playback + ContentIterator), E (full reconciliation + polish). All simulator-testable until Phase C. No download resume v1. 15s position save intervals with adaptive battery scaling. 64KB SyncDelegate background memory limit. Build environment: SDK 9.1.0, Connect IQ manifest requires 32-char hex UUID app IDs, `import Toybox.Lang` (not `using`), developer_key at project root.

- The repo contains an older Tizen watch app (C#/Xamarin) in `PodcastApp/` — the new app will be built for Garmin Connect IQ using Monkey C.
- Garmin watches have significant constraints: limited memory (64KB-256KB app memory depending on device), small screens (240x240 typical), no direct internet access (must proxy through phone via Communications API).
- The existing Tizen app has pages for: Login, Main/Home, Queue, Subscribed Podcasts — these represent the core user flows to replicate.
- Created full Garmin UX spec at `docs/garmin-ux-spec.md` — covers 7 screens, navigation flow, data caching strategy, memory budgets, and companion app requirements.
- Auth on Garmin uses settings-based flow (credentials entered in Garmin Connect Mobile, stored in Application.Properties) since watches have no keyboard.
- Minimum target device: 240×240 round, 128 KB app memory, CIQ 3.2+. Primary targets are Venu 2/3 and Forerunner 265/965.
- v1 uses `Communications.makeWebRequest()` with a lightweight proxy — no custom companion app needed.
- Memory budget: ~95 KB peak for minimum-spec devices. Queue capped at 20 episodes, podcasts at 30, episodes per podcast at 15.
- No podcast artwork in v1 — text-only lists to stay within memory budget.
- Now Playing is a custom View with progress arc, not a Menu2 — it's the only non-menu screen.
- Proposed file structure uses View+Delegate pairs per screen (e.g., `QueueView.mc` + `QueueDelegate.mc`).
- Key open question: Garmin audio playback (Media module) — need to determine stream vs download strategy. Impacts companion architecture.
- **Cross-team update (2026-04-11):** Wash discovered real queue is `/up_next/list` and playback sync is `/sync/update_episode` — these are the exact endpoints the UX targets. Mal's architecture patterns (Menu2, Dictionary models, LRU cache) are compatible with these API endpoints. All three teams aligned on API surface now.
- **Cross-team update (2026-04-12):** Wash completed live API validation (25 endpoints, 20 working). **Critical impacts to UI/UX:**
  1. **Up Next queue is a dictionary, not a list** — `/up_next/list` returns `{ order: [...], episodes: {uuid: {...}} }`. Queue screen must loop over `order` array and lookup titles/metadata from `episodes` dictionary. Update QueueDelegate to bind this structure correctly.
  2. **Episode titles missing from podcast list** — `/user/podcast/episodes` returns ONLY status (playingStatus, playedUpTo, starred, duration). NO titles. If Episodes screen needs titles (it does for UX), must fetch them separately via `POST /user/episode` per episode or bulk `podcast-api.pocketcasts.com`. This breaks the current assumption that `/user/podcast/episodes` is the source of truth for episode metadata. Episode list UX will need a loading state or multi-call strategy.
  3. **Token refresh broken** — `POST /user/token` returns 400. Credentials must be stored on companion and re-login used if session expires. Update auth flow assumptions.
  4. **Search requires Bearer token** — `POST /discover/search` returns 401 without token. If discover feature uses search, ensure token is passed.
  5. **Stats values are strings** — `/user/stats/summary` returns string time values, not numbers. Any stats display must parse these.
- **Build milestone (2026-04-12):** Created the full YoCastsGarmin/ Connect IQ project with mock data service. 17 files, ~900 lines of Monkey C. Five screens implemented (Home Menu2, Queue, Podcasts, Episode List, Now Playing). IPodcastService base class provides the interface — MockPodcastService implements it with 5 realistic podcasts, 18 episodes, and a 5-item queue. Data structures use Dictionary keys matching PocketCasts API fields so the real service can be a drop-in replacement. Now Playing has a progress arc, play/pause, skip ±30s controls. Settings-based auth flow ready (credentials via Garmin Connect Mobile). Need to verify compilation with actual Connect IQ SDK toolchain next.
- **Learned:** Monkey C `getInitialView()` must NOT have explicit return type annotation — SDK will reject override if typed. Menu2 can be returned directly from getInitialView, no wrapper View needed.
- **Note for future:** Up Next queue API returns dict structure (order + episodes map), not simple array. MockPodcastService currently uses simple array — will need to adjust when wiring real API, or normalize in the service layer.
- **Token auth breakthrough (2026-04-12):** Wash investigated `/user/token` 400 error root cause and discovered the two-endpoint token system. `/user/login` returns only access token (no refresh). `/user/login_pocket_casts` returns accessToken + refreshToken + expiresIn. Token refresh via `/user/token` requires `{ "grantType": "refresh_token", "refreshToken": "..." }` body — not Bearer header. This is critical for Garmin auth: login via `/user/login_pocket_casts`, store both tokens + expiry in `Application.Storage`, refresh proactively before expiry, fallback to re-login on 401. Decision proposed in decisions.md.
- **First successful build & simulator run (2026-04-12):** Built YoCastsGarmin with Connect IQ SDK 9.1.0 and deployed to Venu 4 41mm simulator. Required three fixes: (1) Removed invalid `base.device` line from monkey.jungle — device is set via `-d` flag, not jungle config. (2) Changed manifest app ID to 32-char hex UUID — CIQ requires minimum 32-char IDs. (3) Replaced `using Toybox.Lang;` with `import Toybox.Lang;` in all source files — `using` creates an alias but doesn't import types into scope, so `String`, `Number`, `Boolean`, `Array`, `Dictionary` were unresolved. `import` makes them directly available. Keep `using` for aliased imports like `using Toybox.Application as App;`. Also regenerated launcher_icon.png (was a 69-byte placeholder, not a valid PNG). Added try-catch around `App.Properties.getValue()` in `hasCredentials()` to prevent runtime crash when properties aren't populated.
- **Learned:** In Monkey C SDK 9.1.0: `import Toybox.Lang;` imports types directly (String, Number, etc.); `using` only creates aliases and doesn't import type names. Use `import` for Toybox.Lang, `using X as Y` for aliased modules. `import` does NOT support `as` aliasing.
- **Learned:** Connect IQ manifest requires app IDs of at least 32 characters. Use a hex UUID like `a3421feed75247efa2a683e6e5152865`.
- **Learned:** The `base.device` property is NOT valid in monkey.jungle files — specify target device via `-d` flag on monkeyc command line only.
- **Build environment:** SDK bin at `C:\Users\jaert\AppData\Roaming\Garmin\ConnectIQ\Sdks\connectiq-sdk-win-9.1.0-2026-03-09-6a872a80b\bin`. Must set JAVA_HOME and add both SDK bin and Java bin to PATH. Use `.bat` extensions on Windows (monkeyc.bat, monkeydo.bat, simulator.exe). Developer key at `YoCastsGarmin/developer_key`.
- **Device deployment success (2026-04-12T01:22):** App built and deployed to Venu 4 41mm physical device. Simulator test passed. All 5 compilation issues from previous session resolved: (1) Invalid `base.device` removed from monkey.jungle. (2) UUID format fixed in manifest.xml. (3) Converted remaining `using` directives to `import` for Toybox.Lang. (4) Regenerated launcher_icon.png (was corrupted/placeholder). (5) Added try-catch guards around `Application.Properties` reads. App now runs cleanly on physical device. Ready for API integration phase. Orchestration log written to `.squad/orchestration-log/2026-04-12T01-22-kaylee-build.md`.
- **Symbol Not Found crash fix (2026-04-12):** App was crashing immediately on launch with "Symbol Not Found Error: Failed invoking <symbol>". Root cause: `using Toybox.X as X` was used for WatchUi, Graphics, System, and Application modules. While `using` creates an alias, it does NOT import types into scope — so at runtime, the VM couldn't resolve `Menu2`, `View`, `BehaviorDelegate`, `MenuItem`, `Dc`, etc. Fix: converted ALL `using` directives to `import` across all 8 source files. Also updated all `Gfx.` references to `Graphics.` and `App.` to `Application.` since `import` doesn't support aliasing. Added explicit `as String` casts on Dictionary lookups passed as MenuItem id (strict type checker requires this). Removed unused `Background` permission from manifest.xml. Build now passes at `-l 3` (strict) with zero errors. App runs in simulator without crash.
- **Learned (reinforced):** In Monkey C, `using Toybox.X as Y` creates a namespace alias but does NOT make types available in scope. `import Toybox.X` does. This applies to ALL Toybox modules (WatchUi, Graphics, System, Application, Math), not just Lang. Always use `import` — never `using` — unless you specifically only need the alias and won't reference child types directly. The `import` keyword does not support `as` aliasing, so use full module names (e.g., `Graphics.COLOR_BLACK` not `Gfx.COLOR_BLACK`).
- **Login screen redesign (2026-04-12):** Redesigned LoginPromptView for 390x390 AMOLED. Changes: (1) YoCasts title in FONT_LARGE with accent blue (0x55AAFF), (2) "Podcasts on your wrist" subtitle, (3) thin separator lines for visual structure, (4) proper vertical spacing across the round display, (5) white/gray text on black for AMOLED efficiency. Added "Tap to Continue / Demo Mode" skip flow — tapping anywhere or pressing SELECT navigates to home menu with mock data via `WatchUi.switchToView()`. Also changed `hasCredentials()` catch block to return `false` so the login screen always shows when properties aren't set. Build passes strict (-l 3) and deploys to simulator.
- **Learned:** `WatchUi.switchToView()` is the correct way to replace the current view stack (e.g., login → home) without stacking views. `pushView` would stack login underneath. Use `switchToView` for full navigation transitions like login → main app.
- **Learned:** Developer key for builds is at `YoCastsGarmin/developer_key` (relative path), NOT at the AppData path. Use `-y developer_key` when building from the YoCastsGarmin directory.
- **Custom Home Menu UI overhaul (2026-04-12):** Replaced Menu2-based home screen with a fully custom `HomeMenuView` (WatchUi.View) for complete layout control. Four UI improvements implemented:
   1. **Centered pill layout:** Three rounded-rect pills (Queue, Podcasts, Now Playing) centered with 30px margins on 390px AMOLED. No more Menu2 items extending past screen edges.
   2. **Rich informative subtitles:** Queue shows "5 episodes" (dynamic count), Podcasts shows "5 subscriptions" (dynamic count), Now Playing shows episode title + podcast name + progress bar + elapsed/total time.
   3. **Enhanced Now Playing item:** Larger 124px pill with: "NOW PLAYING" label, episode title, podcast name, progress bar, embedded play/pause circle button (tap to toggle), and time display. Play/pause button uses circular hit-test for precise tap detection. Tapping elsewhere on the pill navigates to full Now Playing screen.
   4. **Visual polish with Graphics shapes:** Music note icon (filled circle + stem + flag) on Queue item, headphone icon (arc + ear cups) on Podcasts item, play triangle via `fillPolygon()` and pause bars via `fillRectangle()` on both home menu and Now Playing screen. Replaced text-based icons with proper Graphics-drawn shapes. All icons use 0x55AAFF accent blue on black AMOLED background.
   Also added physical button support (UP/DOWN to cycle items with selection highlight, SELECT to activate) alongside touch. Build passes strict (-l 3). `MainMenuView.mc` (old Menu2 delegate) left in place as dead code — can be cleaned up later.
- **Learned:** `fillPolygon()` in Monkey C takes untyped array literals `[[x1,y1],[x2,y2],[x3,y3]]` — do NOT add `as Array<Array<Number>>` type cast; SDK expects tuple type `Array[Numeric, Numeric]` which differs from `Array<Number>`. Let the compiler infer the type.
- **Learned:** For custom views replacing Menu2 on Garmin, return `[View, BehaviorDelegate]` from `getInitialView()` instead of `[Menu2, Menu2InputDelegate]`. Use `onTap(ClickEvent)` on BehaviorDelegate for touch handling with `getCoordinates()` for hit testing.
- **Cross-team update (2026-04-12):** Wash built real `PocketCastsPodcastService` implementing `IPodcastService`. **Impacts to Garmin dev:**
   1. **IPodcastService interface expanded** — 5 new methods: `isAuthenticated()`, `isDataReady()`, `hasEpisodesForPodcast(uuid)`, `fetchAll()`, `requestEpisodesForPodcast(uuid)`. Original 4 sync getters unchanged. MockPodcastService has no-op stubs for new methods.
   2. **All view type annotations now `IPodcastService`** — Views are service-implementation-agnostic. No code changes needed when switching mock ↔ real.
   3. **Service toggle via `useMockData` property** — Set in Garmin Connect Mobile settings alongside email/password. Controls which service implementation is active.
   4. **Episode list loading UX gap** — Real API fetches episodes on-demand per podcast. Menu2 can't dynamically add items post-construction, so episode list shows "Loading..." on first visit. User must back out and re-enter. Needs view swap mechanism in future polish pass.
   5. **Auth uses `/user/login_pocket_casts`** — Full OAuth2 flow with accessToken + refreshToken + expiresIn. Proactive refresh before API calls. Fallback to re-login on failure.
- **Cross-team update (2026-04-12):** Mal designed offline mode & sync reconciliation architecture (`docs/offline-sync-design.md`). **Impacts to Garmin dev:**
   1. **All Views need offline-aware updates** — connectivity check, offline indicator, gray-out logic for unavailable content.
   2. **Audio download elevated to Phase 3** — no longer a stretch goal. Must prototype `Media.ContentProvider`/`SyncDelegate` for download management.
   3. **Changelog in Application.Storage** — offline mutations recorded in key `"changelog"`, per-episode coalescing. Views that modify playback state must write changelog entries.
   4. **Sync protocol integration** — 7-step push-then-pull state machine. SyncEngine module needed alongside existing service layer.
   5. **Storage budget change** — ~51.5 KB metadata + audio in Media storage (was ~61 KB metadata-only).
- **Phase 1 offline caching implemented (2026-04-12):** Created `CacheManager.mc` (module) and `CachedPodcastService.mc` (decorator class) in `YoCastsGarmin/source/services/`. CacheManager wraps `Application.Storage` with "yc_"-prefixed keys and `cachedAt` timestamps. CachedPodcastService uses the decorator pattern — wraps any `IPodcastService`, loads cached data from Storage on init for instant UI, delegates to the wrapped service when `phoneConnected == true`, and writes fresh data back to cache on the read-through path. TTLs control revalidation (5min queue, 30min podcasts, 1hr episodes) but stale data is always served. `YoCastsApp.mc` updated to wire `CachedPodcastService(PocketCastsPodcastService())` for real API mode; mock mode is unwrapped.
- **Learned:** In Monkey C, `Application.Storage` and `Application.Properties` are separate systems. Properties are set from Garmin Connect Mobile (settings-based, read-only from app). Storage is app-managed key-value persistence. `clearValues()` on Storage does not affect Properties.

- **Download UI implementation (2026-04-16):** Implemented DownloadsView screen with queue status display, progress bars, and action menu. Added Downloads pill to HomeMenuView between Podcasts and Settings. Episode list now shows Download action in context menu. DownloadQueue module interface contract finalized with all required functions for SyncDelegate integration. Interface preserved for backward compatibility with existing DownloadsView. Build passes strict linting on both simulator and device targets. All integration points documented for Wash's real implementation.

- **CIQ image + color feasibility research (2026-04-16):** Completed full research on image loading and custom color support for Venu 4 41mm. **Key findings:** (1) `makeImageRequest()` API works natively for PNG/JPG images with resize options. (2) 30×30px JPG = 1-3 KB per image, 15 thumbnails = ~27 KB in RAM (16-bit color), well within 768 KB budget with lazy loading. (3) Hex colors like `0xFF5500` work directly in `dc.setColor()`. `Graphics.createColor(alpha, r, g, b)` enables semi-transparent overlays. (4) Custom menu item rendering with brand colors is feasible. **Recommendation:** Option A — brand colors first (zero cost), defer thumbnails to Phase E. **Decision:** Colors can be added immediately, thumbnails deferred per Mal's architecture assessment.
- **Custom Home Menu UI overhaul (2026-04-12):** Replaced Menu2-based home screen with a fully custom `HomeMenuView` (WatchUi.View) for complete layout control. Four UI improvements implemented:
  1. **Centered pill layout:** Three rounded-rect pills (Queue, Podcasts, Now Playing) centered with 30px margins on 390px AMOLED. No more Menu2 items extending past screen edges.
  2. **Rich informative subtitles:** Queue shows "5 episodes" (dynamic count), Podcasts shows "5 subscriptions" (dynamic count), Now Playing shows episode title + podcast name + progress bar + elapsed/total time.
  3. **Enhanced Now Playing item:** Larger 124px pill with: "NOW PLAYING" label, episode title, podcast name, progress bar, embedded play/pause circle button (tap to toggle), and time display. Play/pause button uses circular hit-test for precise tap detection. Tapping elsewhere on the pill navigates to full Now Playing screen.
  4. **Visual polish with Graphics shapes:** Music note icon (filled circle + stem + flag) on Queue item, headphone icon (arc + ear cups) on Podcasts item, play triangle via `fillPolygon()` and pause bars via `fillRectangle()` on both home menu and Now Playing screen. Replaced text-based icons with proper Graphics-drawn shapes. All icons use 0x55AAFF accent blue on black AMOLED background.
  Also added physical button support (UP/DOWN to cycle items with selection highlight, SELECT to activate) alongside touch. Build passes strict (-l 3). `MainMenuView.mc` (old Menu2 delegate) left in place as dead code — can be cleaned up later.
- **Learned:** `fillPolygon()` in Monkey C takes untyped array literals `[[x1,y1],[x2,y2],[x3,y3]]` — do NOT add `as Array<Array<Number>>` type cast; SDK expects tuple type `Array[Numeric, Numeric]` which differs from `Array<Number>`. Let the compiler infer the type.
- **Learned:** For custom views replacing Menu2 on Garmin, return `[View, BehaviorDelegate]` from `getInitialView()` instead of `[Menu2, Menu2InputDelegate]`. Use `onTap(ClickEvent)` on BehaviorDelegate for touch handling with `getCoordinates()` for hit testing.
- **Cross-team update (2026-04-12):** Wash built real `PocketCastsPodcastService` implementing `IPodcastService`. **Impacts to Garmin dev:**
  1. **IPodcastService interface expanded** — 5 new methods: `isAuthenticated()`, `isDataReady()`, `hasEpisodesForPodcast(uuid)`, `fetchAll()`, `requestEpisodesForPodcast(uuid)`. Original 4 sync getters unchanged. MockPodcastService has no-op stubs for new methods.
  2. **All view type annotations now `IPodcastService`** — Views are service-implementation-agnostic. No code changes needed when switching mock ↔ real.
  3. **Service toggle via `useMockData` property** — Set in Garmin Connect Mobile settings alongside email/password. Controls which service implementation is active.
  4. **Episode list loading UX gap** — Real API fetches episodes on-demand per podcast. Menu2 can't dynamically add items post-construction, so episode list shows "Loading..." on first visit. User must back out and re-enter. Needs view swap mechanism in future polish pass.
  5. **Auth uses `/user/login_pocket_casts`** — Full OAuth2 flow with accessToken + refreshToken + expiresIn. Proactive refresh before API calls. Fallback to re-login on failure.
- **Cross-team update (2026-04-12):** Mal designed offline mode & sync reconciliation architecture (`docs/offline-sync-design.md`). **Impacts to Garmin dev:**
  1. **All Views need offline-aware updates** — connectivity check, offline indicator, gray-out logic for unavailable content.
  2. **Audio download elevated to Phase 3** — no longer a stretch goal. Must prototype `Media.ContentProvider`/`SyncDelegate` for download management.
  3. **Changelog in Application.Storage** — offline mutations recorded in key `"changelog"`, per-episode coalescing. Views that modify playback state must write changelog entries.
  4. **Sync protocol integration** — 7-step push-then-pull state machine. SyncEngine module needed alongside existing service layer.
  5. **Storage budget change** — ~51.5 KB metadata + audio in Media storage (was ~61 KB metadata-only).
- **Phase 1 offline caching implemented (2026-04-12):** Created `CacheManager.mc` (module) and `CachedPodcastService.mc` (decorator class) in `YoCastsGarmin/source/services/`. CacheManager wraps `Application.Storage` with "yc_"-prefixed keys and `cachedAt` timestamps. CachedPodcastService uses the decorator pattern — wraps any `IPodcastService`, loads cached data from Storage on init for instant UI, delegates to the wrapped service when `phoneConnected == true`, and writes fresh data back to cache on the read-through path. TTLs control revalidation (5min queue, 30min podcasts, 1hr episodes) but stale data is always served. `YoCastsApp.mc` updated to wire `CachedPodcastService(PocketCastsPodcastService())` for real API mode; mock mode is unwrapped.
- **Learned:** In Monkey C, `Application.Storage` and `Application.Properties` are separate systems. Properties are set from Garmin Connect Mobile (settings-based, read-only from app). Storage is app-managed key-value persistence. `clearValues()` on Storage does not affect Properties.
- **Learned:** Decorator pattern works well for Monkey C service layers. The wrapped service's async callbacks call `WatchUi.requestUpdate()`, which triggers view updates. The decorator's read-through getters check the wrapped service on each view cycle and cache new data when detected. No need to intercept callbacks.
- **Home menu UI fix & scrolling (2026-04-13):** Rewrote `HomeMenuView.mc` with proper scrolling viewport, increased spacing (6→20px gaps, 68→72px pills, 124→140px NP), pixel-based text truncation, and scroll-aware hit detection. Added `truncateText()` binary-search helper to `DataModels.mc` using `dc.getTextWidthInPixels()`. Updated `NowPlayingView.mc` to use `truncateText()` instead of character-count truncation. Fixed 24+ pre-existing strict-mode type errors in `PocketCastsPodcastService.mc` (untyped callback `data` params → `Dictionary or String or Null`, `_strOr` val → `Object?`, `_makeAuthPost` body → `Dictionary<Object, Object>`, callback → full Method type) and `CacheManager.mc` (cast dict values to `Application.Storage.ValueType`). Build passes `-l 3` strict.
- **Learned:** Monkey C `-l 3` strict mode: `Communications.makeWebRequest` callbacks must type `data` as `Dictionary or String or Null`. The `_makeAuthPost` body param must be `Dictionary<Object, Object>` — `Dictionary<String, Object>` is NOT a subtype due to invariance. Unparameterized `Dictionary` is also rejected.
- **Learned:** Monkey C `-l 3` strict mode: `Application.Storage.setValue` requires `Dictionary<Storage.KeyType, Storage.ValueType>`. Dict literals with `Array<Dictionary>` values fail because unparameterized `Dictionary` ≠ `Storage.ValueType`. Fix: cast values with `as Application.Storage.ValueType`.
- **Learned:** `dc.setClip(x, y, width, height)` and `dc.clearClip()` are available in CIQ 1.0.0+ and work well for viewport clipping in custom scrolling views. `dc.getTextWidthInPixels(text, font)` enables pixel-accurate text measurement for truncation.
- **Learned:** Monkey C Method type annotations support full signatures: `Method(paramName as Type, ...) as ReturnType`. Use this for `makeWebRequest` callback parameters to satisfy `-l 3`.
- **Home menu UI v2 — tap-to-select & marquee (2026-04-13):** Rewrote `HomeMenuView.mc` to remove `_selectedIndex` highlight. All pills now look uniform — users tap any pill to navigate directly (Queue, Podcasts, Now Playing). Swipe/buttons scroll viewport by pixel offset. Reduced sizing for 390x390 round AMOLED: pillH 72→68, npH 140→105, margin 42→28, gap 20→14, viewportTop 65→55. Added Timer-based marquee text scrolling (150ms tick, 3-phase state machine: pause-start → scroll 2px/tick → pause-end → reset) for overflowing episode/podcast titles in both HomeMenuView and NowPlayingView. Uses `dc.setClip()`/`dc.clearClip()` for per-text-element clipping during marquee. Build passes `-l 3` strict.
- **Learned:** `Timer.Timer.start(method(:callback), periodMs, repeat)` — callback must be a public method (not private) for the method reference to work in Monkey C. Timer fires independently of view lifecycle; use `onShow()`/`onHide()` to start/stop timers to avoid updates on hidden views.
- **Learned:** For marquee text in Connect IQ: use `dc.setClip()` to restrict drawing to the text container, draw left-justified with a negative x-offset, then `dc.clearClip()` (or restore viewport clip). The 3-phase approach (pause → scroll → pause → reset) gives readable results at 150ms intervals.
- **Learned:** Developer key for Connect IQ must be RSA (not EC). Generate with: `openssl genrsa 4096 | openssl pkcs8 -topk8 -inform PEM -outform DER -nocrypt -out developer_key.der`. Store at `%APPDATA%\Garmin\ConnectIQ\developer_key.der`.
- **CRITICAL BUG FIX — Home menu tap selection (2026-04-13):** Root cause identified and fixed. `HomeMenuDelegate` extended `BehaviorDelegate`, which has a behavior translator that converts ALL screen taps into `onSelect()` calls. Since `onSelect()` always navigated to Now Playing and returned `true`, the `onTap()` method (which had correct coordinate-based hit testing) was NEVER called — it was dead code. Fix: changed `HomeMenuDelegate` to extend `WatchUi.InputDelegate` instead of `WatchUi.BehaviorDelegate`. With `InputDelegate`, `onTap(ClickEvent)` fires directly for touch events, so the existing hit-test logic now works correctly. Added `onSwipe(SwipeEvent)` for scroll gestures, `onKey(KeyEvent)` for physical buttons (KEY_ENTER/KEY_START → navigate by `_selectedIndex`, KEY_UP/DOWN → cycle index, KEY_ESC → back). Build passes `-l 3` strict. Deployed to simulator.
- **Learned (CRITICAL):** In Connect IQ, `BehaviorDelegate` converts ALL screen taps into `onSelect()` calls via its behavior translator. If `onSelect()` returns `true`, the event is consumed and `onTap()` NEVER fires. This makes `BehaviorDelegate` unsuitable for custom views that need tap-coordinate hit testing. **Always use `InputDelegate`** for custom drawn menus/views that need `onTap()` with x,y coordinates. Use `BehaviorDelegate` only for simple screens where you want device-agnostic behavior abstraction (like Now Playing, where SELECT = play/pause).
- **Learned:** Connect IQ simulator app settings: **File → Edit Persistent Storage → Edit Application.Properties Data**. This opens a dialog where you can manually set property values (email, password, useMockData, etc.). There is no "Settings" menu in the simulator — the `Settings → Trigger App Settings` menu is only for watch faces/data fields, not watch apps.
- **Layout reference created (2026-04-13):** Created `docs/garmin-layout-reference.md` — pixel-perfect layout spec for all YoCasts screens on the Venu 4 41mm (390×390 AMOLED). Key findings from deep research:
  1. **Round screen geometry:** At Y=65 (top of Queue pill), usable width is only 286px. At center Y=195, full 390px. Created a complete width-at-Y table for layout math.
  2. **Venu 4 has NO flat tire** — fully round display, all pixels usable within the circle.
  3. **Font heights verified for 390×390:** XTINY=33, TINY=41, SMALL=46, MEDIUM=56, LARGE=61, NUMBER_MILD=72, NUMBER_MEDIUM=84, NUMBER_HOT=122, NUMBER_THAI_HOT=144. These are significantly larger than 240×240 devices.
  4. **Adaptive-width pills are essential:** Fixed-width pills that look fine at center get clipped at top/bottom. Each pill's width must be computed from the circle's chord width at that Y position minus margins.
  5. **Home menu fits without scrolling:** 3 pills at 68+68+100px with 12px gaps = 260px content, fitting in 290px viewport. Eliminating scroll removes an entire class of bugs.
  6. **Touch targets:** Industry standard is 44px minimum; Garmin recommends ≥36px. Our pills at 68-100px are well above minimum.
  7. **Garmin UX patterns:** Spotify and built-in music player use vertical scrollable lists with 3-5 visible items, accent color highlights, and hardware button fallbacks. Our pill approach is consistent with this.
- **Learned:** Garmin `drawArc()` angles: 0° = 3 o'clock, 90° = 12 o'clock (top). For a clockwise progress arc starting at top: `dc.drawArc(cx, cy, r, ARC_CLOCKWISE, 90, 90 - progressDegrees)`.
- **Learned:** `dc.setClip(x, y, width, height)` is available since CIQ 1.0.0. Always call `dc.clearClip()` after finishing clipped drawing. Clipping is essential for viewport scrolling and marquee text animation.
- **Learned:** For 390×390 round screens, the max inscribed square is 276×276px (from Y=57 to Y=333). The "safe content zone" with 20px margins is Y=55 to Y=335, where width is always ≥268px.
- **Settings investigation & fix (2026-04-12):** "Trigger App Settings" in CIQ simulator is grayed out **by design** for watch-app types — only watch faces and data fields get that option. The correct way to test settings in the simulator is `File > Edit Persistent Storage > Edit Application.Properties data`. Fixed three issues: (1) Added explicit `base.sourcePath` and `base.resourcePath` to monkey.jungle ensuring properties.xml and settings.xml are discovered. (2) Added `onSettingsChanged()` callback to YoCastsApp so settings changes from Garmin Connect Mobile or simulator propagate immediately (recreates service, triggers fetchAll). (3) Created in-app SettingsView (accessible from 4th home menu pill) showing account status (configured/not) and a toggleable Demo Mode switch. Build passes strict (-l 3), deploys to simulator.
- **AudioContentProviderApp migration complete (2026-04-14):** Phase 0 complete. Migrated app from `AppBase` to `AudioContentProviderApp`, enabling native audio playback. Rewrote `YoCastsApp.mc` base class and added 5 required methods. Created 3 stub files for `YoCastsContentDelegate`, `YoCastsContentIterator`, and `YoCastsSyncDelegate` (extending Communications.SyncDelegate, not Media.SyncDelegate per SDK 9.1.0 constraints). Key findings: SDK rejects explicit `<iq:uses-permission id="Media"/>` (implicitly available for audio-content-provider-app), inlined view construction required (no helper methods due to strict type checking), settings unchanged, SyncConfigurationView deferred to Phase C. Build passes `-l 3` strict. Ready for Phase A (changelog & position tracking).
- **Split-dock home menu spec v2.0 (2026-04-13):** Rewrote Section 5 of `docs/garmin-layout-reference.md` with the new split-dock design. Home screen now has two zones: (1) Scrollable menu zone (Y=0–260) with Queue, Podcasts, and Settings pills at 60px tall with adaptive widths, and (2) Fixed Now Playing dock (Y=260–390, 130px) overlaying the bottom 1/3. Dock shows podcast name (Y=268, XTINY, 322px), episode title (Y=302, XTINY, 286px), 200px progress bar (Y=338, 4px), time display (Y=346, XTINY, 207px), and play/pause button in the dead zone (Y=365–390). Touch zones: dock info area (Y=260–365 → navigate to NP screen), bottom edge (Y=365–390 → toggle playback). Clipping via `dc.setClip(0,0,390,260)` hides scrollable items behind the dock. Added Settings pill with gear icon spec (6-tooth gear drawn with fillCircle + fillRectangle), and Settings page spec using Menu2 with 5 placeholder items. Also added ASCII art diagrams for full screen, dock detail, and scrollable zone.
- **Learned:** CIQ simulator "Trigger App Settings" is only available for watch faces and data fields. For watch-app types, use `File > Edit Persistent Storage > Edit Application.Properties data` to test settings. This is by design, not a bug.
- **Learned:** `monkey.jungle` should explicitly list `base.resourcePath = resources;resources/drawables;resources/strings;resources/settings` to ensure all resource XML files (including settings/properties) are reliably discovered by the SDK. Without explicit paths, the defaults may not recursively scan subdirectories.
- **Deep research: Media module & background services (2026-04-13):** Completed exhaustive research on Garmin Connect IQ's Media module, background services, and power management. Full reference written to `docs/garmin-media-and-background-research.md`. Key discoveries:
  1. **AudioContentProviderApp is a SEPARATE app type** — must extend `AudioContentProviderApp` (not `AppBase`). Launched from music widget, not app list. Different lifecycle: `getContentDelegate()`, `getSyncConfigurationView()`, `getSyncDelegate()`. This is how Spotify/Deezer/Amazon Music work. Our current device app CANNOT do audio download/playback.
  2. **SyncDelegate is THE download mechanism** — `makeWebRequest()` with `HTTP_RESPONSE_CONTENT_TYPE_AUDIO` and `:mediaEncoding` streams audio directly to encrypted disk cache, bypassing the ~32-100KB normal response size limit. Downloads are sequential (chained manually), cannot be parallelized or resumed.
  3. **`Media.SyncDelegate` and `Media.startSync()` are DEPRECATED after System 9** — use `Communications.SyncDelegate` instead (same interface). Programmatic sync triggering replacement is unclear.
  4. **Background services (64KB) CANNOT access Media module** — useful for metadata sync/position push only, not audio downloads.
  5. **Battery/charging API confirmed** — `System.getSystemStats().battery` (Float %), `.charging` (Boolean). Can detect charging state for smart sync triggers.
  6. **Wi-Fi vs Bluetooth CAN be distinguished** — `connectionInfo[:wifi].state` and `connectionInfo[:bluetooth].state` with `CONNECTION_STATE_CONNECTED`. No change callbacks — must poll.
  7. **Supported audio formats: MP3, M4A, ADTS, WAV. OGG NOT supported.**
  8. **Temporal events: minimum 5-minute interval**, max 1 event registered at a time.
  9. **Venu 4 has 8 GB storage, ~3.5-4 GB usable for audio after system files.**
  10. Constraints document written to `.squad/decisions/inbox/kaylee-media-constraints.md` — the app type question needs team decision before implementation.
- **Learned (CRITICAL):** Audio download/playback on Garmin requires the `AudioContentProviderApp` app type, not a regular device app. The app is launched from the music widget, uses `getSyncConfigurationView()` for episode selection, `Communications.SyncDelegate` for downloads, and `ContentDelegate` for playback event tracking. Cannot be combined with a regular device app in a single binary.
- **Learned:** `makeWebRequest()` with `:responseType => Communications.HTTP_RESPONSE_CONTENT_TYPE_AUDIO` and `:mediaEncoding => Media.ENCODING_MP3` streams audio to encrypted media cache. The callback receives a `ContentRef` (not raw data). This bypasses normal response size limits. Must be used within SyncDelegate context for reliability.
- **Learned:** `Media.getCacheStatistics()` returns `CacheStatistics` with `.capacity` and `.size` (both Long, in bytes). Use this to check available media storage before downloading episodes.
- **Learned:** `Media.ContentMetadata` has 5 fields: `title`, `artist`, `album`, `genre` (all String), `trackNumber` (Number). For podcasts: title=episode title, artist=podcast name, album=podcast name, genre="Podcast".
- **Learned:** `Media.PlaybackProfile` controls native media player UI. For podcasts: set `skipForwardTimeDelta=30`, `skipBackwardTimeDelta=15`, `requirePlaybackNotification=true`, `playbackNotificationThreshold=30`. Playback controls array order determines button layout; first item may be used as hotkey.
- **Learned:** `System.getDeviceSettings().connectionInfo` is a `Dictionary<Symbol, ConnectionInfo>` with keys `:wifi`, `:bluetooth`, `:lte`. Each value has `.state` property (CONNECTION_STATE_NOT_INITIALIZED=0, NOT_CONNECTED=1, CONNECTED=2). This allows distinguishing Wi-Fi from Bluetooth for smart sync decisions.
- **Learned:** Background `ServiceDelegate` temporal events have a 5-minute minimum interval (verified from API docs: "cannot be set to occur less than 5 minutes after the last temporal event"). Only 1 temporal event can be registered at a time. Background services can access `Application.Storage` and `makeWebRequest()` but NOT Media module, timers, sensors, or UI.
- **Split-dock home menu v2.0 IMPLEMENTED (2026-04-13):** Complete rewrite of `HomeMenuView.mc` to implement the two-zone split-dock design from `docs/garmin-layout-reference.md` Section 5. Zone 1 (Y=0–260): scrollable menu with "YoCasts" title at Y=12 (FONT_MEDIUM, 0x55AAFF) and 3 adaptive-width pills — Queue (221px at X=85, Y=50–110), Podcasts (322px at X=34, Y=122–182), Settings (332px at X=29, Y=194–254). Each pill has icon at pillX+16/pillY+10, primary text at pillX+48/pillY+8 (FONT_SMALL white), sub text at pillX+48/pillY+34 (FONT_XTINY 0xAAAAAA). Zone 2 (Y=260–390): fixed Now Playing dock with divider (1px, 0x333333), podcast name (Y=268, XTINY, 0xAAAAAA, 322px max, marquee), episode title (Y=302, XTINY, white, 286px max, marquee), progress bar (Y=338, 200px centered, 4px, 0x333333/0x55AAFF), time (Y=346, XTINY, 0xAAAAAA), play/pause icon (195, 378, 0x55AAFF). Touch zones: Y≥365 = play/pause, Y≥260 = NP screen, Y<260 = pill hit test. Draw order exactly per Section 5.8. Gear icon uses 6-tooth spec from Section 5.5.6. Removed old 4-pill scrollable layout entirely. Build passes `-l 3` strict with zero errors.
- **Home menu UX polish (2026-04-13):** Addressed 4 issues from simulator testing: (1) **Taller pills** — PILL_HEIGHT 60→80px for breathing room. (2) **Uniform pill width** — replaced 3 per-pill widths (221/322/332px) with single PILL_WIDTH=280px centered at X=55. All pills now look identical in size. (3) **Removed Settings subtitle** — gear icon + "Settings" only, no "Playback, downloads" sub-line; vertically centered in the taller pill. (4) **Natural scrolling** — YoCasts title now scrolls WITH content (not fixed), Settings pill starts below dock (Y=272) requiring scroll to reach, total content height 382px vs 260px viewport = 122px scroll range always active. Added scroll indicators (up/down triangles) that appear contextually. Exposed `getScrollOffset()` on view so delegate does accurate hit-testing at any scroll position. Content-space Y: title=25, Queue=80, Podcasts=176, Settings=272. Build passes `-l 3` strict.
- **Learned:** On 390×390 round Garmin displays, pills drawn beyond the inscribed circle are naturally clipped by the hardware's round display mask. This is cosmetically acceptable and standard — no need to compute per-pill widths from circle geometry. A uniform width centered on screen works well and the round mask provides a natural visual edge.
- **Phase A modules & polish completed (2026-04-12T21:49:00Z):** Two background agents (kaylee-phase-a, kaylee-polish-screens) delivered Phase A integration-ready code:
  1. **ConnectivityManager module** — three-state connectivity (OFFLINE, PHONE_ONLY, WIFI_AVAILABLE) tracking Venu 4's direct Wi-Fi + phone BLE proxy scenarios. Used by CachedPodcastService for smart sync strategies. Location: `YoCastsGarmin/source/managers/ConnectivityManager.mc`.
  2. **ChangeLog module** — per-episode coalescing of offline mutations. Stores latest action/playedUpTo/status per episode. Idempotent for sync replay. Cleared after confirmed server push. Location: `YoCastsGarmin/source/modules/ChangeLog.mc`, Storage key `"yc_changelog"`.
  3. **CachedPodcastService hardening** — TTL validation, selective cache clear (not `clearAll()`), connectivity-aware behavior (OFFLINE=cache-only, PHONE_ONLY=batch-update, WIFI=eager revalidation). Decorator pattern unchanged, internal improvements only.
  4. **UI polish** — Queue/Episodes screens now show status indicators (▶/⏸/✓), Now Playing redesigned with progress arc visualization and three-button controls (back/play-pause/skip), centralized InputDelegate for consistent button navigation across all screens.
  5. **All builds pass `-l 3` strict.** Ready for Phase A (changelog & position tracking) implementation.


- **Session orchestration (2026-04-12T15:20:00Z):** Completed parallel background research on Garmin Media module and split-dock UI implementation and documentation audit.
- **Azure Function proxy integration (2026-04-14):** Updated `PocketCastsPodcastService.mc` to route all data-fetching API calls through an Azure Function proxy at `PROXY_BASE`. Login and token refresh stay direct to `api.pocketcasts.com` (we need the token FROM login). All other calls (`_fetchPodcasts`, `_fetchQueue`, `_enrichNextQueueItem`, `_fetchNextEpisodeDetail`, `requestEpisodesForPodcast`) go through the proxy which strips heavy fields (description, descriptionHtml, etc.) to stay under the CIQ ~32KB response limit. Removed `:maxLength => 65536` from `_makeAuthPost` since the proxy guarantees small responses. `PROXY_BASE` is a const at the top of the class for easy URL changes. Build passes `-l 3` strict.
- **Learned:** CIQ `Communications.makeWebRequest()` has a ~32KB response size limit. PocketCasts API responses (especially podcast lists with descriptions) can exceed this. An Azure Function proxy that strips unused heavy fields is the recommended pattern — the proxy forwards the Bearer token as-is, no credentials stored server-side.

- **Cross-team update (2026-04-14):** Split-dock home menu implementation APPROVED. Mal's app type migration decision APPROVED — HomeMenuView survives as primary browsing surface, served via `getPlaybackConfigurationView()`. Phase 0 (1 day) validates AudioContentProviderApp on Venu 4 simulator. All inbox decisions merged to `decisions.md`. Next phase: prototype AudioContentProviderApp + SyncConfigView.
- **AudioContentProviderApp migration COMPLETE (2026-04-14):** Successfully migrated YoCasts from `AppBase` to `AudioContentProviderApp`. Changes:
  1. **manifest.xml:** `type` changed from `watch-app` to `audio-content-provider-app`. No `Media` permission needed — SDK rejects it as invalid; Media module is implicitly available for audio-content-provider-app types.
  2. **YoCastsApp.mc:** Base class changed to `Application.AudioContentProviderApp`. Added 5 new methods: `getPlaybackConfigurationView()`, `getSyncConfigurationView()`, `getContentDelegate()`, `getSyncDelegate()`, `getProviderIconInfo()`. All three config view methods return HomeMenuView (or LoginPromptView if no credentials). View construction inlined in each method because the strict type checker requires tuple return types (`[Views] or [Views, InputDelegates]`) and rejects untyped helper methods returning `Array`/`Any`.
  3. **3 new stub files in source/media/:** `YoCastsContentDelegate.mc` (extends `Media.ContentDelegate` — logs playback events), `YoCastsContentIterator.mc` (extends `Media.ContentIterator` — returns null for all content, includes podcast-optimized PlaybackProfile with ±30s/15s skips), `YoCastsSyncDelegate.mc` (extends `Communications.SyncDelegate` — returns `isSyncNeeded()=false`, calls `notifySyncComplete` immediately).
  4. **monkey.jungle:** Added `source/media` to sourcePath.
  5. **Build passes clean at `-l 3` strict.** All existing views, services, and models unchanged.
- **Learned (CRITICAL):** `getSyncDelegate()` in SDK 9.1.0 requires `Communications.SyncDelegate`, NOT `Media.SyncDelegate`. The strict type checker enforces return type `Null or Communications.SyncDelegate`. MonkeyMusic sample's use of `Media.SyncDelegate` is outdated.
- **Learned:** AudioContentProviderApp view methods (`getPlaybackConfigurationView`, `getSyncConfigurationView`) enforce strict tuple return types at `-l 3`. You cannot delegate to a helper method that returns untyped `Array` — the compiler infers `Any` and rejects it. Must inline view construction in each method.
- **Learned:** `Media` is NOT a valid permission ID in Connect IQ manifest.xml. The SDK rejects `<iq:uses-permission id="Media"/>` with "Invalid permission provided: Media". Media module access is implicit for `audio-content-provider-app` types.
- **Settings verification (2026-04-14):** Confirmed settings.xml and properties.xml are correct for the new app type. PocketCastsEmail uses `alphaNumeric` type, PocketCastsPassword also uses `alphaNumeric` (no `password` type exists in CIQ — input is always clear text). useMockData uses `boolean` type. These settings work identically in audio-content-provider-app as in watch-app — `Application.Properties` API is unchanged.
- **CRITICAL BUG FIX — Simulator crash after AudioContentProviderApp migration (2026-04-14):** App crashed on launch with "Symbol Not Found Error / Error starting music app" and simulator showed "No Media Found". Root cause: AudioContentProviderApp type has a fundamentally different launch flow — apps appear in the music widget (not app list), and the system calls `getContentDelegate()` → `getContentIterator()` → `get()` to play content. Our `ContentIterator.get()` returned `null` (no downloaded audio), crashing the native media player. Garmin's MonkeyMusic sample handles this by showing "No songs on the system" in the playback config view, but the core issue is that audio content provider apps REQUIRE downloaded media to function — they are not standalone browseable apps. **Fix:** Reverted to `watch-app` (AppBase) type: manifest type → `watch-app`, YoCastsApp base class → `AppBase`, removed all audio provider methods, excluded `source/media` from build. Media stub files preserved for Phase C when audio download is implemented. Build passes `-l 3` strict.
- **Learned (CRITICAL):** AudioContentProviderApp is NOT suitable for apps without downloaded audio content. The native media player requires `ContentIterator.get()` to return a valid `ContentObj` or the player crashes. You cannot use this app type for "browse-only" or "coming soon" audio features — content must actually exist in the media cache. Use `watch-app` (AppBase) for browsing, then switch to `audio-content-provider-app` only when SyncDelegate can actually download and cache audio files.
- **Learned:** To test a `watch-app` in the CIQ simulator: (1) Build with `monkeyc.bat`, (2) Launch simulator with `simulator.exe`, (3) Load app via `monkeydo.bat bin\YoCasts.prg venu441mm`. The app appears directly on the watch face. For `audio-content-provider-app` types, the launch flow is different: the app appears as a music provider in the music widget, and you navigate to it through Music controls.
- **Learned:** Garmin's MonkeyMusic sample (`garmin/connectiq-apps` on GitHub, `audio-provider/monkeymusic/`) is the canonical reference for AudioContentProviderApp implementation. Key patterns: `ConfigurePlaybackView` checks for songs before showing playlist menu, `ContentIterator.initializePlaylist()` uses `Media.getContentRefIter()` to enumerate cached content, and the app pre-seeds an auth token to bypass OAuth during testing.
- **RE-MIGRATION TO AudioContentProviderApp (2026-04-14):** Successfully re-migrated to `audio-content-provider-app` with proper empty-state handling. Three research docs (Mal, Kaylee, Wash) all independently confirmed: (1) AudioContentProviderApp IS the only way to play audio, (2) the earlier crash was our fault — not a platform limitation, (3) ContentIterator.get() returning null is valid and handled gracefully by the native player ("No Media"). Key changes: manifest type → `audio-content-provider-app`, YoCastsApp extends `AudioContentProviderApp`, removed `getInitialView()` entirely (audio providers don't use it), added `getPlaybackConfigurationView()` as the auth-gated entry point, `getSyncConfigurationView()` delegates to the same view, `getContentDelegate(arg)` returns `YoCastsContentDelegate` directly (not in array), `getSyncDelegate()` returns `YoCastsSyncDelegate`, `getProviderIconInfo()` uses `new Media.ProviderIconInfo(resourceId, color)` constructor. Build passes clean at `-l 3` strict.
- **Azure Function proxy integration (2026-04-14):** Updated `PocketCastsPodcastService.mc` to route all data-fetching API calls through an Azure Function proxy at `PROXY_BASE` URL. Wash built `YoCastsProxy/` (new .NET 8 isolated worker project in solution) that strips heavy fields from responses to stay under the CIQ ~32KB limit. Changes: (1) Added `PROXY_BASE` constant at top of PocketCastsPodcastService.mc; (2) All data calls use `PROXY_BASE` (via `_makeAuthPost`); (3) Login and token refresh stay direct to `API_BASE` — we need the Bearer token FROM login; (4) Removed `:maxLength => 65536` from `makeWebRequest` calls since proxy guarantees small responses; (5) Class docstring updated to document proxy architecture. Proxy endpoints mirror PocketCasts paths (`/api/pocketcasts/user/podcast/list`, etc.), whitelist fields (uuid, title, author, artwork_url, author_color, duration, published_at, episodes_sort_order, num_episodes, last_sync_time, in_rotation), forward Bearer token as-is. Build passes `-l 3` strict. Deploy proxy and update PROXY_BASE URL for real API testing.
- **Learned (CORRECTED):** AudioContentProviderApp CAN work without downloaded content. The earlier crash was NOT caused by ContentIterator.get() returning null — that's valid API behavior. The crash was likely caused by: (a) overriding getInitialView() which conflicts with the audio provider launch flow, (b) the API 6.0 Symbol Not Found simulator bug, or (c) incorrect return types from provider methods. The MonkeyMusic sample and garmin-podcasts open-source app both handle empty content gracefully. Previous learning about "AudioContentProviderApp requires downloaded media" was WRONG.
- **Learned:** `getContentDelegate(arg)` must return the delegate object directly, NOT wrapped in an array. `getPlaybackConfigurationView()` and `getSyncConfigurationView()` return arrays. `getProviderIconInfo()` uses constructor: `new Media.ProviderIconInfo(Rez.Drawables.Icon, colorInt)`.
- **Learned:** Audio content provider apps do NOT appear in the watch's app list. To test in simulator: build with monkeyc.bat, load with monkeydo.bat, then in the simulator navigate to Music Controls → Music Providers → select YoCasts. The system calls `getPlaybackConfigurationView()` which shows our HomeMenuView.
- **Dual-build configuration implemented (2026-04-14):** CIQ simulator CANNOT run `audio-content-provider-app` types — confirmed Garmin platform limitation. Set up two-jungle dual-build system so Jarod can develop UI in the simulator and deploy audio to hardware. Structure:
  - `source/app/YoCastsApp.mc` — device entry (extends `AudioContentProviderApp`, uses `getPlaybackConfigurationView()`)
  - `source/sim/YoCastsApp.mc` — simulator entry (extends `AppBase`, uses `getInitialView()`)
  - `monkey.jungle` → `manifest.xml` (audio-content-provider-app) → `source/app` + `source/media`
  - `monkey.simulator.jungle` → `manifest.simulator.xml` (watch-app) → `source/sim` (no media stubs)
  - All shared code (views, services, models) used by both builds unchanged.
  - Both builds pass clean at `-l 3` strict. README updated with build commands.
- **Learned (CRITICAL):** Connect IQ `sourcePath` in monkey.jungle IS recursive — subdirectories are automatically included. Cannot put alternate app entry points under `source/` or they'll conflict. Solution: separate sibling directories (`source/app/` for device, `source/sim/` for simulator) and reference only the needed one in each jungle file.
- **Learned:** JDK 21 (Eclipse Temurin) installed at `C:\Program Files\Eclipse Adoptium\jdk-21.0.10.7-hotspot`. Must set `JAVA_HOME` and add `$JAVA_HOME\bin` to PATH before running monkeyc.bat.
- **View screens polished to match home menu design language (2026-04-14):** Updated 4 views + 1 bug fix:
  1. **QueueView:** Title "Up Next" → "Queue" for consistency with home menu pill. Sublabel now shows "Played" status for completed episodes and uses `|` separator instead of `•` for cleaner reading.
  2. **EpisodeListView:** Sublabel now shows explicit status: "Played | duration" for completed, "elapsed / duration" for in-progress, "duration | New" for unplayed. Clear at-a-glance status differentiation.
  3. **NowPlayingView (major overhaul):** Redesigned per layout reference §7. Progress arc enlarged (r=185, pen=6) with accent blue (0x55AAFF) instead of system COLOR_BLUE. Episode title upgraded to FONT_MEDIUM for prominence. Added 3-button control row at Y=245: skip back (−15s, rewind icon), play/pause (r=32, accent blue), skip forward (+30s, fast-forward icon). Skip buttons in pill-colored (0x1A1A2E) circles. Time display moved to Y=300 with FONT_TINY. Delegate switched from BehaviorDelegate to InputDelegate for tap-coordinate hit testing on all 3 buttons + swipe-right to go back + onKey for physical buttons.
  4. **SubscribedView:** Reviewed, already clean — author subtitle is appropriate, no changes needed.
  5. **ChangeLog.mc bug fix:** Removed `private` keyword from `_evictOne()` — `private` is not valid inside Monkey C modules. Pre-existing bug that blocked builds.
- **Learned:** `private` access modifier is NOT valid inside Monkey C `module` blocks. Modules only support `function` (public by default). Classes support `private`, `protected`, `public`. This will cause a compile error at any lint level.
- **Phase A connectivity & changelog (2026-04-14):** Built Phase A from audio-download-implementation-plan. Three deliverables:
  1. **ConnectivityManager module** (`source/services/ConnectivityManager.mc`): Three-state detection — STATE_WIFI (connectionAvailable && !phoneConnected), STATE_PHONE (connectionAvailable && phoneConnected), STATE_OFFLINE (!connectionAvailable). Four public functions: `getState()`, `isConnected()`, `isWifiDirect()`, `canMakeRequests()`. All do live `System.getDeviceSettings()` reads — no polling timer needed for Phase A.
  2. **ChangeLog module** (`source/services/ChangeLog.mc`): Offline mutation log in Application.Storage key "yc_changelog". Three change types (POSITION_UPDATE, EPISODE_COMPLETED, QUEUE_REMOVE). Coalesces POSITION_UPDATEs per episode. Max 100 entries with eviction (oldest non-completion first). Four public functions: `addEntry()`, `getEntries()`, `clearEntries()`, `getEntryCount()`.
  3. **CachedPodcastService fix:** `_isConnected()` now delegates to `ConnectivityManager.isConnected()` instead of raw `phoneConnected || connectionAvailable`. `fetchAll()` uses `ConnectivityManager.getState()` for state-aware refresh (scaffold for future WIFI-vs-PHONE differentiation).
  4. **CacheManager.clearCache() safety fix:** Replaced `clearValues()` with selective `deleteValue()` calls for known cache keys, protecting ChangeLog entries and future auth storage from accidental wipe.
  Build passes clean at `-l 3` strict.
- **Learned:** `System.getDeviceSettings().connectionAvailable` returns true if ANY internet path exists (Wi-Fi OR Bluetooth proxy). This is the correct primary check for "can make HTTP requests." Using `phoneConnected` alone misses Wi-Fi direct connectivity. Using `phoneConnected || connectionAvailable` is redundant — `connectionAvailable` alone suffices.
- **Simulator settings bug fix (2026-07-15):** Investigated and fixed "No settings file found for this app" error in CIQ simulator.
  - **Root cause:** `monkeydo` does NOT automatically deploy the `*-settings.json` file that `monkeyc` generates alongside the `.prg`. It only auto-detects and deploys the `.debug.xml` file. The VS Code Monkey C extension handles this automatically via its Debug Adapter Protocol integration, but CLI usage requires explicit deployment.
  - **Fix:** Use monkeydo's `/a` (additional files) flag: `monkeydo bin/YoCasts.prg venu441mm /a bin/YoCasts-settings.json 0:/GARMIN/APPS/YoCasts-settings.json`
  - **How it works:** `ShellUtils.pushPrg()` pushes PRGs to `0:/GARMIN/APPS/<name>`. For additional files ending in `-settings.json`, `MonkeyDoDeux` uppercases the app-name portion before pushing to the simulator's virtual filesystem.
  - **Created:** `deploy-sim.bat` helper script that wraps build + deploy with settings in one command.
  - **Updated:** README.md with correct monkeydo syntax and explanation of the `/a` flag requirement.
- **Learned:** The CIQ simulator uses a virtual filesystem with paths like `0:/GARMIN/APPS/` (PRGs), `0:/GARMIN/Debug/` (debug XML). Settings files must be explicitly pushed to `0:/GARMIN/APPS/<APPNAME>-settings.json` (uppercased). The monkeydo `/a` flag takes pairs of args: `<source> <dest>` (bat joins them with `;` internally).
- **CIQ Image & Color feasibility research completed (2026-07-15):** Full report at `docs/ciq-image-color-feasibility.md`. Key findings:
  1. **`makeImageRequest()` confirmed working** — downloads PNG/JPG/YUV from URLs at runtime (API 1.2.0+). Callback returns `BitmapResource or BitmapReference or Null`. Options: `:maxWidth`, `:maxHeight`, `:packingFormat` (JPG recommended for artwork). Venu 4 supports all packing formats (API 4.2.0+).
  2. **Images CANNOT be persisted** — `Application.Storage` only accepts `ValueType`. Downloaded bitmaps live in graphics memory only. Must re-download each app session. `BitmapReference.isCached()` detects eviction.
  3. **Custom RGB colors confirmed** — hex integers like `0xFF5500` work directly as color arguments to `dc.setColor()`. `Graphics.createColor(alpha, r, g, b)` creates ARGB colors (API 4.2.0+). Venu 4 has 16-bit display (65,536 colors) so rendered colors are approximated.
  4. **Memory math: 15 thumbnails at 30×30px = ~27 KB (16-bit)** — well within the Venu 4's 768 KB budget. Even with 32-bit alpha, only ~53 KB. Total app footprint would be ~143 KB, leaving ~625 KB headroom.
  5. **Alpha blending supported** — `BLEND_MODE_SOURCE_OVER`, `Graphics.createColor()` with alpha, `ALPHA_BLENDING_FULL` for BufferedBitmaps. Can overlay white text on semi-transparent brand-color backgrounds.
  6. **`fillRoundedRectangle()` available** — already used in HomeMenuView for pills. Works for colored list item backgrounds.
  7. **`CustomMenuItem` (API 3.2.0+)** — allows fully custom draw per Menu2 item. Alternative: convert to full custom View.
  8. **`drawBitmap2()` (API 4.2.0+)** — supports `:tintColor`, `:filterMode` (bilinear), `:transform` (AffineTransform). Use for high-quality thumbnail rendering.
  9. **Recommended phases:** Phase 1 = brand colors only (zero memory cost, zero network). Phase 2 = artwork thumbnails (progressive download, ~27 KB). Phase 3 = polish (bilinear filtering, shimmer loading state).
- **Learned:** `Communications.makeImageRequest()` loads images from URLs at runtime. Returns `BitmapResource or BitmapReference or Null` in callback. Use `:maxWidth/:maxHeight` to constrain downloaded size. `PACKING_FORMAT_JPG` recommended for photographic content (smallest transfer). Images cannot be stored in `Application.Storage` — graphics memory only.
- **Learned:** `Graphics.createColor(alpha, r, g, b)` creates ARGB color values (API 4.2.0+). Alpha 0=transparent, 255=opaque. Works with `dc.setColor()` for semi-transparent overlays. `dc.setBlendMode(Graphics.BLEND_MODE_SOURCE_OVER)` enables standard alpha compositing.
- **Learned:** `WatchUi.CustomMenuItem` (API 3.2.0+) allows fully custom drawing per item in a Menu2. Override `draw(dc)` for complete control over item appearance. This is the bridge between Menu2's scroll/selection handling and custom visual rendering.
- **Learned:** `dc.drawBitmap2(x, y, bitmap, options)` (API 4.2.0+) supports `:tintColor`, `:filterMode` (POINT/BILINEAR), `:transform` (AffineTransform), and sub-region rendering. Use `FILTER_MODE_BILINEAR` for smooth scaled thumbnails.
- **Learned:** Venu 4 41mm has 768 KB foreground app memory, NOT 128 KB. The earlier "no artwork in v1" decision was based on minimum-spec 240×240 devices. For the Venu 4 single-target build, 15 thumbnails at 30×30px 16-bit = ~27 KB is trivially within budget.
- **Download UI screens implemented (2026-04-14):** Built 4 components for download management UX:
  1. **DownloadQueue.mc** (source/services/) — Stub module with mock data (4 episodes: 1 downloaded, 1 downloading@45%, 1 pending, 1 failed) and full API contract (addToQueue, removeFromQueue, getStatus, getProgress, toEpisodeDict). Uses in-memory array — real impl will use Application.Storage. Interface designed so Wash can drop in the real download engine.
  2. **DownloadsView.mc** (source/views/) — Custom scrollable View with 80px items. Each item shows status icon (checkmark/progress ring/clock/X), episode title, podcast name, color-coded status label, and progress bar for active downloads. Empty state with download icon + message. Toast notification system for feedback. Swipe-left to remove, swipe up/down to scroll, tap to play (downloaded) or show status.
  3. **HomeMenuView.mc** — Added Downloads pill at Y=272 (between Podcasts@176 and Settings@368). Shows download count badge (e.g., "4 episodes"). Download arrow icon (shaft + arrowhead + U-shaped tray). Updated TOTAL_MENU_HEIGHT from 382→478, adjusted delegate hit zones and navigateByIndex for 4 pills.
  4. **EpisodeListView.mc** — Episode selection now shows action Menu2 with "Play" and "Download" options. Download option is context-aware: shows "Downloaded" (✅), "Downloading" (%), "In Queue", or "Download" based on DownloadQueue state. Status indicators appended to episode sublabels. New EpisodeActionDelegate handles Play/Download selection.
  Both simulator and device builds pass `-l 3` strict.
- **Learned:** Monkey C modules (like `DownloadQueue`) are singletons — their `var` declarations persist for the app lifetime. Module-level variables work well for in-memory state that doesn't need to survive app restart (mock data, UI state). For persistence, use `Application.Storage`.
- **Phase C Audio Download implemented (2026-07-16):** Full SyncDelegate + ContentIterator implementation for background audio download and native media player playback.
  1. **YoCastsSyncDelegate.mc** — Complete login→AudioInfo→download→store pipeline. Background service context authenticates with PocketCasts independently (reads email/password from Properties, caches token in Storage). Sequential downloads: for each pending episode, fetches CDN URL from AudioInfo proxy, downloads audio via `makeWebRequest(HTTP_RESPONSE_CONTENT_TYPE_AUDIO)`, stores ContentRef in StorageManager. Reports progress via `Media.notifySyncProgress()`. Handles cancel cleanly (resets to PENDING for retry).
  2. **YoCastsContentIterator.mc** — Builds playlist from DownloadQueue STATUS_DOWNLOADED items, looks up refIds via StorageManager, returns ContentObj via `Media.getCachedContentObj(ContentRef)`. Podcast playback profile: 30s forward skip, 15s back skip, playback notifications.
  3. **manifest.xml** — Added Background and PersistedContent permissions.
  4. **DownloadsView.mc** — Added `StorageManager.removeDownload()` call when removing episodes from queue (was missing cleanup).
- **Learned:** `makeWebRequest` with `HTTP_RESPONSE_CONTENT_TYPE_AUDIO` returns `PersistedContent.Iterator` (not `Media.ContentRef` directly). Must call `iter.next()` to get the ContentRef, then `contentRef.getId()` for storage. Requires `PersistedContent` permission in manifest.xml.
- **Learned:** CIQ callback signatures for `makeWebRequest` must be typed as `data as Dictionary or String or PersistedContent.Iterator or Null` — using `data as Any` or untyped parameters fails at default type check level. The compiler validates callback signatures against the expected PolyType.
- **Learned:** `Communications.SyncDelegate` (not `Media.SyncDelegate`) is the correct base class for sync in System 9+ / SDK 9.1.0. Media.SyncDelegate is deprecated.
- **Learned:** ContentIterator methods (`get()`, `next()`, `previous()`, `peekNext()`, `peekPrevious()`) must return `Media.Content?` with explicit type annotations. `canSkip()` returns `Boolean`, `shuffling()` returns `Boolean`, `getPlaybackProfile()` returns `Media.PlaybackProfile?`.
- **Learned:** Device build at default type check level (`monkeyc` without `-l`) only catches permission and real type errors. `-l 3` also catches inherited method scope issues in the existing codebase that are pre-existing. Phase C code compiles cleanly at default level.
- **Learned:** Unicode emoji characters (✅ ⏳ ❌ ↓) work in Menu2 sublabels rendered by Garmin's native font system, but availability depends on device font support. Safe to use in text but may render as boxes on some older devices.
- **Brand color tinting implemented (2026-07-15):** Added podcast brand color support to the Garmin app. Changes across 4 files: (1) DataModels.mc — new `P_ART_COLOR`, `P_ART_TINT`, `P_ART_URL` DataKeys plus `parseHexColor()`, `hexPairToDec()`, and `dimColor()` helpers in DataFormat. (2) PocketCastsPodcastService.mc — `_transformPodcast()` now parses `artColor`/`artTint` hex strings from proxy response and converts to CIQ integer colors. (3) MockPodcastService.mc — all 5 demo podcasts have realistic brand colors. (4) SubscribedView.mc — converted from plain `MenuItem` to `PodcastMenuItem` (extends `CustomMenuItem`) with dimmed brand-color backgrounds (20% unfocused, 35% focused), full-brightness initial circle, and vertically-centered title/author text. Zero additional memory cost (colors are just integers). Build passes `-l 3` strict.
- **Learned:** `CustomMenuItem` (CIQ 3.2.0+) enables per-item custom drawing in Menu2. Override `draw(dc)` for full rendering control. Use `isFocused()` for focus-aware highlighting. Call `setLabel(title)` in initialize so `MenuItem.getLabel()` still works for navigation delegates. The system may draw a default focus indicator on top — manageable since our dimColor background already handles focus state visually.
- **Learned:** CIQ accepts any 24-bit integer as a color in `dc.setColor()`. No need for `Graphics.COLOR_*` constants. Hex color parsing from strings (#RRGGBB) requires manual per-character conversion since Monkey C has no built-in hex parser. For AMOLED, dim brand colors to 20-35% brightness to avoid eye strain and burn-in — multiply each RGB channel by a 0.0-1.0 factor via `dimColor()`.

- **[deploy-sim.ps1 created]** Converted deploy-sim.bat to idiomatic PowerShell (deploy-sim.ps1). Added auto-start for the CIQ simulator — script checks for running simulator process, launches simulator.exe from SDK bin with a 5-second init wait if not found. Same build+deploy flow, same device target (venu441mm), same SDK/key paths, same settings file handling. Old .bat kept alongside for compatibility. Simulator executable confirmed as simulator.exe in SDK bin dir.
- **Brand color tinting expanded to all list views (2026-04-19):** Extended brand color tinting beyond SubscribedView to QueueView and EpisodeListView. Changes: (1) DataModels.mc — added `luminance()`, `ensureContrast()` (contrast safety: returns white/black if fg/bg luminance diff < 0.25), and `lookupPodcastColors()` (looks up artColor/artTint by podcast UUID from subscribed list, returns [0x333333, 0xFFFFFF] default). (2) SubscribedView.mc — PodcastMenuItem now accepts artTint and uses it for title color (contrast-checked) and dimmed-tint for author (0.65 factor). (3) QueueView.mc — new QueueEpisodeMenuItem (CustomMenuItem) with brand-colored accent bar on left edge, dimmed brand background (15%/30% focus), tint-colored title. Colors looked up per episode via E_PODCAST_UUID → lookupPodcastColors(). (4) EpisodeListView.mc — new EpisodeMenuItem (CustomMenuItem) with status indicator dots (filled=new/in-progress, ring=completed), subtle brand background (10%/22% focus), parent podcast colors applied uniformly. All fallback gracefully to white-on-dark when color data missing. Device build passes `-l 3` strict.
- **Learned:** `DataFormat.lookupPodcastColors()` is the canonical way to resolve podcast brand colors by UUID. Returns `[artColor, artTint]` array. O(n) scan over subscribed podcasts — acceptable at n≤30 cap. Null-safe: handles missing DataKeys gracefully.
- **Phase A — ChangeLog enhanced + PositionTracker implemented (2026-07-16):** Enhanced existing `ChangeLog.mc` module with Phase A convenience API and created new `PositionTracker.mc` class. Changes: (1) ChangeLog.mc — added `TYPE_STATUS_CHANGE` and `TYPE_QUEUE_ADD` constants alongside existing types. Added 5 convenience methods: `logPositionUpdate(uuid, podcastUuid, position, duration)`, `logStatusChange(uuid, podcastUuid, status)`, `logQueueAction(uuid, podcastUuid, action)`, `getChangelog()`, `clearChangelog()`. Reduced `MAX_ENTRIES` from 100 to 50 for 8 KB budget compliance. Position updates auto-coalesce per episode (existing behavior). (2) PositionTracker.mc — new class in `source/services/`. Timer-based 15s position sampling with battery-adaptive scaling (30s when battery < 20%). Logs to both ChangeLog (for sync) and CacheManager (for instant offline resume). Skips redundant writes when position unchanged. Lifecycle: NowPlayingView creates instance on init, calls `startTracking(self)` on play, `stopTracking()` on pause/exit. `logNow()` for immediate logging on skip or pause. (3) NowPlayingView.mc — integrated PositionTracker. Creates tracker in `initialize()`, starts on play, stops on pause/hide, force-logs on skip forward/back. Added `getCurrentPosition()` public accessor. Build passes `-l 3` strict. Foundation ready for Phase B sync engine.
- **Learned:** Monkey C classes work for timer callbacks (`method(:onPositionTick)` resolves correctly), but modules cannot use `method(:symbol)` since there is no `self` in module context. Use classes when Timer ownership is needed. Module-level `var` is also avoided in existing codebase — all mutable state goes through `Application.Storage` in modules.
- **Learned:** ChangeLog storage key is `"yc_changelog"` (with yc_ prefix to match CacheManager convention). PositionTracker writes to both ChangeLog (for eventual sync push) and CacheManager (for immediate offline resume via `savePlaybackPosition`). Dual-write ensures position data survives both network outages and app restarts.
- **Phase B — SyncEngine implemented (2026-07-18):** Created `SyncEngine.mc` class in `source/services/`. Full 7-step push-then-pull state machine: (1) Auth check — reads credentials from Properties, does own login via `/user/login_pocket_casts`, caches token. (2) Read changelog — snapshots entries + IDs to isolate from PositionTracker writes during sync. (3) Fetch server state — hybrid bulk: `/user/in_progress` first, then individual `/user/episode` for gaps. (4) Reconcile — aggregates local state per episode, applies "furthest wins" for position and COMPLETED > IN_PROGRESS > NOT_PLAYED hierarchy for status. (5) Push — sequential `/sync/update_episode` per changed episode via callback chaining. (6) Refresh caches — triggers `service.fetchAll()` for full data pipeline re-fetch. (7) Cleanup — selectively clears only snapshot entries from changelog, preserving entries added during sync and entries for failed pushes. Integrated into YoCastsApp: sync triggered on `onStart()` and each `getPlaybackConfigurationView()` call. Guards: no-op if already syncing, offline, or changelog empty. Pure reconciliation functions (`reconcilePosition`, `reconcileStatus`, `reconcileQueue`) for testability. Device build passes `-l 3` strict.
- **Learned:** SyncEngine is a class (not module) because `method(:callback)` requires class instance context for `makeWebRequest` async callbacks. Module-level functions cannot be referenced via `method(:symbol)` in Monkey C.
- **Learned:** Race condition between sync cleanup and PositionTracker solved by: (a) snapshotting changelog at step 2 with entry IDs, (b) at cleanup, filtering current entries to keep only those NOT in snapshot (new during sync) or for failed-push episodes. Direct `Application.Storage.setValue(ChangeLog.KEY_CHANGELOG, ...)` used since ChangeLog has no `removeByIds()` method.
- **Learned:** For reconciliation, multiple changelog entries per episode (e.g., POSITION_UPDATE + EPISODE_COMPLETED) are aggregated before reconciling with server: max(position), max(status via hierarchy), max(duration). This ensures COMPLETED status and furthest position always win regardless of entry order.

**[2026-04-19 Cross-Agent Sync]**
- **From Wash:** Audio download confirmed **NO auth required** for CDN access. All 5 tested CDNs support Range headers (206 Partial Content). SupportingCast premium URLs have JWT embedded in URL — re-fetch before each download. AudioInfo proxy deployed to `yocasts-proxy-personal.azurewebsites.net`. Use endpoint `GET /api/pocketcasts/episode/{uuid}/audio-info` to fetch: `audioUrl` (final CDN URL after redirects), `fileSize` (via HEAD request), `requiresAuth` (SupportingCast flag), `contentType` (audio/mpeg). Response ~515 bytes — well under Garmin 2KB per-value limit.
- **From Mal:** Phase C gate: (1) Background permission in manifest (`<iq:uses-permission id="Background"/>`), (2) Venu 4 hardware acquisition + testing. All phase A/B prerequisites met. Top risks documented: 64 KB SyncDelegate memory limit, HTTP_RESPONSE_CONTENT_TYPE_AUDIO behavior unverified on hardware, CDN redirect handling, interrupted downloads. Hardware testing plan ready (`docs/hardware-testing-plan.md`).

**[2026-04-19 Phase D — Media Playback Integration]**
- **Phase D implemented:** ContentDelegate fully wired with onSong() event state machine, 15s position timer, ChangeLog/CacheManager integration, episode completion logging. ContentIterator extended with parallel UUID tracking, setCurrentByUuid() for episode selection, reverse refId→UUID lookup. New PlaybackState module provides shared playback state between ContentDelegate (device native playback) and NowPlayingView (sim mock playback). DownloadsView now resumes from cached position and supports retry for failed downloads. EpisodeListView loads cached position for downloaded episodes. Both device and simulator builds pass.
- **Learned:** In AudioContentProviderApp, the system's native music player controls playback — the app provides content via ContentDelegate/ContentIterator and receives events via onSong(). The app CANNOT programmatically start/stop playback. onSong(contentRefId, songEvent, playbackPosition) fires on state changes (start=0, pause=1, resume=2, complete=3, stop=4), not at regular intervals. Supplementary in-app NowPlayingView is for display/local control only.
- **Learned:** ContentDelegate should be cached as a singleton in YoCastsApp (not recreated per getContentDelegate() call) to preserve timer state and current-track context between system calls.
- **Learned:** PlaybackState module pattern: shared mutable state (module-level vars) works well for cross-component communication in CIQ. ContentDelegate writes from native playback events, NowPlayingView writes from local mock playback, HomeMenuView dock reads for display. getEstimatedPosition() accounts for elapsed time between updates.
- **Learned:** For position logging between onSong() events, use Timer + PlaybackState.getEstimatedPosition() which calculates lastPosition + (now - lastEventTime). This gives reasonable 15s logging even when system events are sparse.
- **Architecture note:** Device build uses source/app/YoCastsApp.mc (AudioContentProviderApp with Media module), sim build uses source/sim/YoCastsApp.mc (AppBase, no Media). Shared code in views/services must not import Toybox.Media. PlaybackState module is Media-agnostic — safe for both builds.
- **[2026-04-19 Brand Color Fix]** CustomMenuItem.draw() was not rendering colors in simulator. Two fixes applied: (1) Added explicit {:height => 80} to all CustomMenuItem.initialize() calls — without it, some CIQ SDK versions may not invoke draw() at all. (2) Added DataFormat.brightenColor(color, targetMax) function that proportionally scales a color so its brightest channel reaches targetMax, preserving hue. This is critical because PocketCasts artColor values are extremely dark (e.g., #1d2b38 = RGB 29,43,56) — dimColor at 20% produces RGB(5,8,11), invisible on AMOLED black backgrounds. New dim factors: backgrounds 0.25-0.60 (up from 0.10-0.35), circles boosted to targetMax=140, accent bars to 160. Debug System.println added to every draw() to confirm execution.
- **[2026-07-15 CustomMenuItem ROOT CAUSE FOUND & FIXED]** The real reason `CustomMenuItem.draw()` was never called: **CustomMenuItem belongs in `CustomMenu`, NOT in `Menu2`.** The Garmin CIQ API docs explicitly state: "A CustomMenuItem is a element of a CustomMenu View" and "A CustomMenuItem can be added to a CustomMenu using the addItem() method." When CustomMenuItems are added to a regular Menu2 via addItem(), Menu2 ignores their draw() override and falls back to rendering the label text from setLabel(). **Fix:** Converted SubscribedView, QueueView, and EpisodeListView from `extends Menu2` to `extends CustomMenu`. CustomMenu.initialize() takes `(itemHeight, backgroundColor, options)` — item height is set at the menu level (80px), not per-item. Removed invalid `{:height => 80}` from CustomMenuItem.initialize() options (not in CustomMenuItem's API). Added `drawTitle(dc)` overrides for custom title rendering. Created EmptyStateMenuItem (extends CustomMenuItem) for empty/loading states since CustomMenu.addItem() only accepts CustomMenuItem. Added comprehensive logging through entire data pipeline. Build passes `-l 3` strict.
- **Learned (CRITICAL):** In Connect IQ, `CustomMenuItem.draw()` is ONLY invoked when the item lives inside a `WatchUi.CustomMenu`, not a regular `WatchUi.Menu2`. CustomMenu extends Menu2 but adds the custom rendering pipeline. If you put CustomMenuItems into Menu2, they render as plain text items using setLabel(). This is the single most important CIQ menu API distinction.
- **Learned:** `CustomMenu.initialize(itemHeight, backgroundColor, options)` — first arg is default item height (Number), second is background color (ColorType), third is options dict. The `:titleItemHeight` option allocates space for a custom title drawn via the overridable `drawTitle(dc)` method. Menu2's `{:title => "string"}` does NOT work for CustomMenu — you must override `drawTitle(dc)` instead.
- **Learned:** `CustomMenuItem.initialize(id, options)` — valid options are `:alignment`, `:drawable`, `:icon`, `:dividerIcon`. There is NO `:height` option. Item height comes from the parent CustomMenu's `itemHeight` parameter. Passing unrecognized options like `{:height => 80}` is silently ignored.